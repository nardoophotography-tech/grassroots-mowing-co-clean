node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local Claude Code config (per-machine dev launch settings)
.claude/

# One-off codemod scripts and source backups (clutter)
*.backup
*.backup-*
*.backup2
*.backup3
