import { calculateBookingPrice } from './src/utils/pricing.ts'; const packages = ['town_block', 'standard_yard', 'corner_blocks', 'large_lot_acreage', 'ultimate_property_gold', 'custom_quote']; console.log('Using default fallback settings:'); packages.forEach(pkg => { const result = calculateBookingPrice(pkg, 'one_off', null); console.log(pkg + ': subtotal=' + result.snapshot?.subtotal?.toFixed(2) + ' gst=' + result.snapshot?.gst?.toFixed(2) + ' total=' + result.estimatedTotal.toFixed(2) + ' isQuoteRequired=' + result.isQuoteRequired); });
