# GrassRoots Mowing Co - Safe Start Notes

Use this for the app frontend:

.\START-FRONTEND.ps1

Use this to check if the app builds:

.\CHECK-FRONTEND-BUILD.ps1

Do not run backend/server commands unless we are specifically working on the backend.

The frontend app is here:

C:\Users\nardo\Desktop\mowing\grassroots-mowing-conew\client

Frontend URL:

http://localhost:5173/

Current best rule:

- For app UI work: run frontend only.
- For backend/API work: stop frontend first, then we fix backend separately.
