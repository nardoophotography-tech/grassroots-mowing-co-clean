rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow authenticated users to read and write
      // In production, we'd harden this to specific paths per user/job
      allow read, write: if request.auth != null;
    }
  }
}
